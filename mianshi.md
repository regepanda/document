最大公约数
找出所有公有的约数 球最大的
/辗转除法,互相除,如果能除开,可以约了
UDP/TCP
有连接无连接,
系统资源
结构简单
流模式和数据报模式
保证正确性和顺序性

线程安全如何保证
加锁,不同语言有细化的不同策略

TCP三次/四次


TCP最后一次握手丢失
有定时期方法上哦你SYN+ACK 再发一次一段时间后直接超时,但会


TCP防止SYN attack
半连接存活时间 队列长度 反复重传
做一个代理以客户的名义回一个
防火墙超时检查,超时了丢一个Rst
防火枪代理服务器回复ACK并建立连接,建立好了,再回一个

网络层工作
本层通过寻址来建立两个节点之间的连接，为源端的运输层送来的分组，选择合适的路由和交换节点，正确无误地按照地址传送给目的端的运输层。 它包括通过互连网络来路由和中继数据 ；除了选择路由之外，网络层还负责建立和维护连接，控制网络上的拥塞以及在必要的时候生成计费信息。常用设备有交换机；

TCP最大连接数
linux取决于系统,且每个进程有最大打开文件描述符,可以根据这  个来判定

进程和线程区别
进程有独立的内存空间,线程没有,是共享的

一个要应用开多个进程
linux fork

多线程如何分配内存


sleep和wait

主动调用线程Run方法,会怎么样


STring的Equal原理

抽象类和接口
抽象类可以实现一部分方法,接口只能有方法的声明,接口可以通过imple

范型实现

反射的缺点

100个人生日同一天

快排

二分

AB线程底层如何实现B为等待A而阻塞

红黑数 val

HashMap HashTable

网络分层 协议头

滑动窗口

数据库索引

交换两个数不用中间变量